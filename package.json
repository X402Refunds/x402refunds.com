{
  "name": "agent-court",
  "version": "0.1.0",
  "description": "Agent Court MVP - Agentic Government for AI Agents",
  "private": true,

  "scripts": {
    "dev": "cd apps && pnpm dev",
    "deploy": "cd apps && pnpm deploy",
    "build": "echo '✅ Pure Convex backend - no build needed'",
    "lint": "echo '✅ No linting needed for Convex functions'",
    "type-check": "echo '✅ TypeScript handled by Convex'",
    "test": "echo '✅ No tests configured yet'"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "turbo": "^1.11.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
