{
  "name": "@agent-court/convex-backend",
  "version": "0.1.0", 
  "private": true,
  "description": "Convergence AI - Pure Convex Backend for Agent Government OS",
  "scripts": {
    "dev": "convex dev",
    "build": "echo 'Convex backend - no build needed'",
    "deploy": "convex deploy",
    "lint": "echo 'No linting configured for Convex backend'",
    "type-check": "echo 'TypeScript checking handled by Convex'"
  },
  "dependencies": {
    "convex": "^1.26.2",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
