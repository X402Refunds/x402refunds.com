import { redirect } from "next/navigation";

export default function PitchDeckPage() {
  // Server-side redirect - instant, no intermediate page
  redirect("https://consulate-dispute-resolu-bkibmf4.gamma.site/");
}