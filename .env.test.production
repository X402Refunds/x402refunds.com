# Test Environment Configuration - PRODUCTION
# ⚠️ READ-ONLY tests only! Never write to production!

# Convex Production Deployment
CONVEX_DEPLOYMENT=prod:perceptive-lyrebird-89
CONVEX_URL=https://perceptive-lyrebird-89.convex.cloud

# API Endpoints
API_BASE_URL=https://api.consulatehq.com
FRONTEND_BASE_URL=https://consulatehq.com

# Test Configuration
NODE_ENV=production
USE_LIVE_API=true

# Test API Key (for MCP authentication tests)
# NOTE: This should be a PRODUCTION key (csk_live_* prefix)
# Only used for READ-ONLY operations in smoke tests
TEST_API_KEY=csk_live_4f1e395fddbe4447d0532ece6314b6b3
