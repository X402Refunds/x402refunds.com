# Test Environment Configuration - PRODUCTION
# ⚠️ READ-ONLY tests only! Never write to production!

# Convex Production Deployment
CONVEX_DEPLOYMENT=prod:perceptive-lyrebird-89
CONVEX_URL=https://perceptive-lyrebird-89.convex.cloud

# API Endpoints
API_BASE_URL=https://api.x402refunds.com
FRONTEND_BASE_URL=https://x402refunds.com

# Test Configuration
NODE_ENV=production
USE_LIVE_API=true

# NOTE: Do not put secrets in this file. CDP/Alchemy creds live in Convex env vars.
